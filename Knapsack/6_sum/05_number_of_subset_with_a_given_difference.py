# You have to divide array into 2 subsets such that
# s1 - s2 should be equal to given difference.
#
# s1 - s2 = diff
# s1 + s2 = sum(arr)
#
# 2 * s1 = diff + sum(arr)
#
# s1 = (diff + sum(arr)) // 2
#
# so you have to find number of subsets whose sum is s1. Boils down to prob 2

